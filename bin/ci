#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "local_ci_plus"

CI = LocalCiPlus::ContinuousIntegration
CI.run do
  step "Linting", "bundle exec standardrb"
  step "Tests", "bundle exec ruby -Itest test/all_tests.rb"
end
